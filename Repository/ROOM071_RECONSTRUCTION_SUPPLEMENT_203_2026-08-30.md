# ROOM 071 — RECONSTRUCTION SUPPLEMENT 203 — 2026-08-30

Status: `CLOSED / RESUME-SAFE`
Lease: `R71-20260830-P2-EJR-PATH-BOUND-AUTHORITY-REVIEW-203`
Functional head: `311d1b7e6928a47bd45df594b65ffe7aa3797b71`

## What was resolved
Five path-bound EJR ambiguity groups were reviewed at the level of competing record content, current consumers, consumer authority/status, and semantic role.

Bounded result:

`EJR-211 / 214 / 219 / 301 / 302 = DISTINCT LEGITIMATE IDENTITY-REUSE COLLISIONS / CONTEXTUAL REFERENTS RECOVERED / NO GLOBAL OWNER ASSIGNED`.

The former question “which duplicate is the real record?” is now too coarse for these groups. The evidence proves that multiple legitimate records were created under the same EJR number for different engineering/session purposes.

## Recovered contextual referents
- `EJR-211`: P29 governed/canonical-memory provenance selects the Memory P29 record.
- `EJR-214`: P31 governed/canonical-memory provenance selects the Memory P31 record.
- `EJR-219`: P36 governed/canonical-memory provenance selects the Memory P36 record.
- `EJR-301`: REP-021 selects the Root GT-040 record as its bounded checkpoint referent; the earlier Memory P6 record remains independently legitimate.
- `EJR-302`: REP-022 selects the Root GT-041 record; GOV-013B/P221 semantically selects the separate Root CI-decision-boundary learning record; the earlier Memory P6/current-HEAD diagnostic is independently distinct. This is a same-surface plus cross-surface triple collision.

## Authority conclusion
`REFERENT RECOVERY != IDENTITY OWNERSHIP`.

A governed consumer can prove which record it means without proving which historical artifact must retain the reused identifier after repair.

## Exact-head functional evidence
At `311d1b7e6928a47bd45df594b65ffe7aa3797b71`:
- Full-Stack Repository Audit `33326986664` — `SUCCESS`.
- ARGO Runtime Prototype and Integration Tests `33326986700` — `SUCCESS`.
- M2 Multi-Channel Proposal Training `33326986683` — `SUCCESS`.
- Real Mutation Matrix Regression `33326986675` — `SUCCESS`.

No standalone Internal Document-ID run is claimed for this Repository-doc-only functional scope because none was observed at the exact head.

## Retrieval learning
The first immediate exact-head Actions query returned zero runs. It was rejected as an absence proof. A broader branch/event listing recovered the exact-head runs, and the direct exact-head endpoint later returned the same four runs successfully.

Learned rule:

`AN IMMEDIATE ZERO-RUN ACTIONS LOOKUP IS NOT EVIDENCE THAT NO RUN EXISTS WHEN AN INDEPENDENT ACTIONS SURFACE CAN STILL RECOVER THE EXACT-HEAD RUN.`

No unsupported internal GitHub cause is asserted.

## Other learned rules
1. `A DUPLICATE ID CAN REPRESENT MULTIPLE LEGITIMATE RECORDS; VALID CONTENT DOES NOT MAKE REUSED IDENTITY VALID.`
2. `A CURRENT GOVERNED CONSUMER MAY RECOVER A CONTEXTUAL REFERENT WITHOUT GRANTING GLOBAL OWNERSHIP OF THE IDENTIFIER.`
3. `WHEN TWO DIFFERENT GOVERNED CONTEXTS SEMANTICALLY RESOLVE THE SAME ID TO DIFFERENT RECORDS, THE DEFECT IS IDENTITY REUSE, NOT CONTENT DUPLICATION.`
4. `SAME-SURFACE COLLISIONS CAN BE AS REAL AS CROSS-SURFACE COLLISIONS.`
5. `IDENTITY REPAIR MUST PRESERVE BOTH LEGITIMATE CONTENT AND THE CONSUMER-PROVENANCE EDGE THAT IDENTIFIES IT.`

## Preserved boundaries
- no EJR mutation, migration, rename, delete, reassignment, normalization, suppression, replacement allocation, or canonical promotion;
- REP-012, REP-016, REP-020 unchanged;
- scanner/gate semantics unchanged;
- Priority 2 historical/provenance identity scope remains `OPEN`;
- active indexed canonical uniqueness remains previously `CLOSED/PASS`;
- Phase 1 remains `OPEN`;
- Release Priority 20 remains `CLOSED_FOR_PHASE_1`;
- Global Connected Baseline remains `OPEN`;
- Provider Authentication remains `HARD HOLD` where no trust anchor exists;
- Memory full-folder integrity remains `NOT CERTIFIED`;
- Global `BOOTED / INTEGRITY PASS` remains `NOT CLAIMED`.

## Resume target
Rediscover live `main`, re-enter from this checkpoint, then build a separate controlled identity-repair plan for these proven identity-reuse collisions before any mutation.

The plan must, for each legitimate record:
1. decide whether the reused number is retained or reassigned based on authority, chronology, and consumer/provenance obligations;
2. prove every replacement EJR candidate vacant using the Lease-193 complete-history vacancy gate;
3. enumerate exact consumers and required path/identity rewrites;
4. preserve historical chronology and semantic content;
5. identify REP/index/manifest synchronization obligations before execution;
6. execute no renumbering merely because a number appears unused.

This checkpoint authorizes planning only, not identity repair execution.
